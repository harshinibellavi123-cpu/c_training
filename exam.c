#include <stdio.h>
void main(int m1,int m2,int m3)
{
    int marks;
    printf("enter the marks");
    scanf("%d",&marks);
    if((m1>=40) && (m2>=40) && (m3>=40))  
    {
        printf(congratulaions,marks>=40);
    }
    else
    {
        printf(better next time,marks<=40);
    }